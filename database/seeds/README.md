# Seeds

Seed data is generated by a TypeScript script (needs password hashing and referential
inserts across multiple tables), not plain SQL. Run it with:

```bash
npm run seed --workspace apps/api
```

See [apps/api/src/scripts/seed.ts](../../apps/api/src/scripts/seed.ts).
